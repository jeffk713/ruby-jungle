<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <% if enhanced_cart.empty? %>
    <%= render 'show_without_item' %>
  <% else %>
    <%= render 'show_with_item' %>
  <% end %>
</section>
